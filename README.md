# fe-news
## 1주 개발 리스트

### 1. 기본 구조 설계 html(그리드)
- [x] 시멘틱 태그로 주요 탭 구성
- [x] 상단에 뉴스스탠드 로고, 시스템 날짜 배치
- [x] 최신 뉴스 자동롤링탭 2개 배치
- [x] 자동롤링탭 각 좌측에 언론사, 우측에 기사 타이틀
- [x] 하단에 가로 6, 세로 4의 그리드 배치
- [x] 그리드상단에 전체 언론사, 내가 구독한 언론사, 구독언론사 갯수 표기, 리스트 로고, 그리드 로고 배치
- [x] 각 그리드의 셀에는 언론사의 로고 배치
- [x] 셀에 hover할 시 표시되는 +구독하기 태그
- [x] 그리드의 좌측, 우측에 화살표 배치
- [x] 다크모드 버튼 배치 위치 정하기

### 2. 레이아웃 구성(CSS)
- [x] 뉴스스탠드 로고, 시스템 날짜는 같은 행에, 각각 좌우 양끝에 배치(space-between 사용?)
- [x] 최신 뉴스 자동롤링탭 2개를 1행에 배치, 각각 border와 background 설정
- [x] 전체 언론사, 내가 구독한 언론사, 언론사 개수를 좌측에 배치
- [x] 리스트 로고, 그리드 로고를 우측에 배치
- [x] 그리드의 각 셀 중앙에 로고 배치
- [x] 셀 hover시 기존 언론사 로고 숨김, +구독하기 태그 표시
- [x] 그리드의 좌우측 중앙에 화살표 배치
- [ ] 다크모드에 따른 테마 변경

### 3. 구독 기능
- [x] 언론사 그리드에 자동 배치
- [x] 구독 기능 추가
- [x] 구독한 언론사를 따로 구독 탭에서 확인할 수 있도록 구현
- [x] 전체 언론사 탭, 구독 언론사 탭 전환

### 4. 롤링 탭
- [x] 각 롤링 탭에 기사 5개씩 추가
- [x] 롤링 탭 CSS활용 overflow: hidden으로 기사 1개씩만 노출되도록 조절
- [x] 롤링 탭 인덱스 값 조절로 기사 전환
- [x] 롤링 탭 애니메이션 추가
- [x] 마우스 호버 시 기사 전환 중지
<br><br>

## 2주 개발 리스트

### 1. Grid ↔ List 전환 구조 설계
- 뉴스 스탠드 UI를 Grid / List 뷰로 전환할 수 있도록 설계
- 탭 클릭 시 기존 DOM을 유지하지 않고 container.innerHTML = '' 기반으로 뷰를 완전히 교체하는 방식 채택
- 상태는 subscriptionStore에서 관리하고, 렌더링은 view 단위로 분리하여 DOM 상태 의존성 제거

### 2. Grid 렌더링 로직 점검 및 디버깅
- renderGrid 함수에서 DOM이 제거되지 않는 이슈 확인
- 원인이 selector 오타임을 확인하고 수정
- innerHTML = '' 방식이 정상적으로 동작함을 재확인

### 3. 데이터 가공: category 중복 제거 로직 구현
- 언론사 데이터에서 category 값만 추출
- Set을 활용해 중복 없는 category 목록 생성
- 카테고리 탭 / 필터 UI에 활용 가능한 형태로 정리
```js
const categories = [...new Set(
  presses.map(press => press.category)
)];
```

## 수정 작업
### 1. 기존 롤링 탭 수정
- hover시 롤링 중지로 인한 1초 딜레이가 지켜지지 않는 문제 수정
  - [x] 두 롤링 탭의 전체 주기를 관리할 하나의 클락 생성(rollingClock.js)
  - [x] 각 탭당 DOM 2개만 유지하고 롤링 시 데이터 인덱스를 기준으로 DOM을 업데이트하도록 개선
  - [x] 전역 클락의 tick 이벤트를 각 롤링 탭이 구독하는 구조로 변경하여 hover 이후에도 항상 동일한 기준 시간에서 롤링이 재개되도록 처리
  - [x] 좌우 영역에 offset 값을 적용하여 두 롤링 탭이 항상 1초의 시간차를 유지하며 롤링되도록 구현
  - [x] hover 상태에서는 해당 영역의 롤링만 일시 정지하고 전역 클락은 유지하여 다른 영역의 롤링에는 영향을 주지 않도록 분리
  - [x] hover 해제 시 별도의 시간 계산이나 타이머 재생성 없이 다음 클락 tick에 자동으로 재동기화되도록 설계

### 2. 뉴스 데이터 fetch 방식 수정
- 기존에는 listCategory.js 등 개별 컴포넌트에서 뉴스 데이터를 각각 로드하고 있었음
- 이 구조는 리스트, 그리드, 롤링 탭 등 여러 UI에서 동일한 데이터를 사용할 경우 중복 fetch 및 데이터 불일치 가능성이 있음
- 이를 해결하기 위해 앱 초기 진입 시(app.js) 뉴스 데이터를 한 번만 fetch하도록 변경
- 로드된 데이터는 newsStore.js에서 관리하며, 각 UI 컴포넌트는 store의 API를 통해 데이터를 참조하도록 역할을 분리함
- 이를 통해 데이터 로드 책임을 중앙화하고, 추후 그리드·필터·구독 기능 확장 시에도 일관된 데이터 흐름을 유지할 수 있도록 개선함


## 해야할 작업
### 1. 카테고리 롤링 탭 구현
- [ ] 카테고리 목록에서 좌/우 이동 가능한 롤링 탭 UI 구현
- [ ] 현재 선택된 카테고리 active 상태 처리
- [ ] 롤링 시 선택된 카테고리 유지
- [ ] 카테고리 변경 시 뉴스 목록 필터링 연동

---

### 2. List View 뉴스 렌더링
- [ ] `renderList.js` 구현
- [ ] 리스트 형태로 뉴스 아이템 표시
  - [ ] 언론사 로고
  - [ ] 언론사명
  - [ ] 구독 버튼
- [ ] Grid View와 동일한 구독 UX 유지

---

### 3. 페이지네이션 기능
- [ ] 페이지 버튼 클릭 시 다음 뉴스 목록으로 이동
- [ ] 현재 페이지 상태 관리
- [ ] 카테고리 변경 시 페이지 초기화 여부 정의
- [ ] Grid / List View 전환 시 페이지 상태 처리 기준 정리

---

### 4. 비동기 처리 적용
- [ ] 뉴스 데이터 로딩 로직 비동기화
- [ ] `fetch` 또는 async/await 기반 데이터 처리
- [ ] 로딩 중 상태(UI) 처리 여부 검토
- [ ] 비동기 데이터 수신 후 렌더 함수 호출 구조 정리
 
    - 비동기 처리는 **뉴스 데이터를 받아오는 단계**에서 적용하는 것이 적절한지 검토 필요

---

### 5. List View 구독 필터
- [ ] List View에서 구독한 언론사만 보기 기능 추가
- [ ] 구독 필터 ON/OFF UI 구현
- [ ] 구독 상태 변경 시 리스트 자동 갱신
- [ ] Grid View와 구독 필터 동작 기준 통일

---

### 6. 상태 관리 정리
- [ ] 현재 View 상태 (`grid / list`) 관리
- [ ] 선택된 카테고리 상태 관리
- [ ] 현재 페이지 상태 관리
- [ ] 구독 상태 변경 이벤트와 렌더링 연동 정리